org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Kotlin's incremental compiler cache crashes on Windows when the project
# drive (e.g. D:) differs from the Pub cache drive (e.g. C:) -- it can't
# compute a relative path across drive roots. Disable incremental Kotlin
# compilation to avoid it (slightly slower rebuilds, but builds succeed).
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
